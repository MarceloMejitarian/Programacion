#include "stdfax.h"
#include "conio.h"
#include "vector"
#include <iomanip>
#include <iostream>
using namespace std;

void main()
{
 do
   {
    char product[] = { " Resistencia " , " Bobina " , " Condensador " };
    int op1, op2, Pro; 
    
    cout << " 1.- Ingreso de datos ";
    cout << " 2.- Hacer pedidos ";
    cout << " 3.- Mostrar informe ";
    cout << " 4.- Salir ";
    cin >> op1;
    switch (op1) 
     {
       case 1:
             {
               cout << " 5.- Resistencia ";
               cout << " 6.- Bobina ";
               cout << " 7.- Condensador ";
               switch (op2)
                 {
                   case 5:  
                          { 
                            int i, n[1001] = {0};
                            char product [0] = Resistencia;
                            cout << " Ingrese la cantidad ";
                            cin >> Pro;
                            for ( i = 0; i < 1001; i++)
                                 n [i] = 0;
                            
                            // vector de precio
                             cout << " Ingrese precio unitario ";
                            
                            float tolerac [3] = { 0.01, 0.05, 0.10}; 
                            cout << " Ingrese la tolerancia ";
                            cin >> Tol;
                            if ( Tol == tolerac[0])
                            cout << tolerac[0];
                              else 
                                  {
                                    if (Tol == tolerac[1])
                                    cout << tolerac[1];
                                    {
                                     else 
                                        { 
                                         if (Tol == tolerac[2])
                                         cout << tolerac[2];
                                        }
                                    }
                                  }
                          }        
                   case 6: 
                           {
                             int Bob;
                             char product [1] = Bobina;
                             cout << " Ingrese la cantidad ";
                             cin >> Bob;
                             for ( i = 0; i < 1001; i++)
                                  n[i] = 0;
                                  
                             // vector de precio     
                              cout << " Ingrese precio unitario ";
                              
                             
                             
                           }   
                           
                           
                   case 7:
                           {
                           
                             int Cond;
                             char product [2] = Condesador;
                             cout << " Ingrese la cantidad ";
                             cin >> Cond;
                             for ( i = 0; i < 1001; i++)
                                  n[i] = 0;
                                  
                             // vector de precio     
                              cout << " Ingrese precio unitario ";
                           }
                      
                           
       case 2:
              {
                cout << " Que tipo de producto necesita ";
                cin >> Tip;
                if 
                          
                      
       
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
